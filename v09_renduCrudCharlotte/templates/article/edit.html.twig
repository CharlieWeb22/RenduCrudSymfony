{% extends 'base.html.twig' %}

{% block title %}Modifier l'Article{% endblock %}

{% block body %}

    <div class="container">
        <h1 class="text-center mt-3 mb-5">Modifier un article</h1>
        <div class="col-lg-12"
            {{ include('article/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

            <a class="btn btn-success float-right mt-3"  href="{{ path('article_index') }}">Retour à la liste</a>

            {{ include('article/_delete_form.html.twig') }}
        </div>
    </div>

{% endblock %}
