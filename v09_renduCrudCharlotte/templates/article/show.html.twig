{% extends 'base.html.twig' %}

{% block title %}Article{% endblock %}

{% block body %}
<body>
    
        <div class="container">
            <a class="btn btn-success float-right mt-3"  href="{{ path('article_index') }}">Retour à la liste</a>
            {% if is_granted('ROLE_ADMIN') %}
            <a class="btn btn-warning float-right mx-1 mt-3" href="{{ path('article_edit', {'id': article.id}) }}">Modifier</a>                                      
            <a class="float-right mt-3">{{ include('article/_delete_form.html.twig') }}</a>
            <p class="mt-3 btn btn-dark">Identifiant de l'article : {{ article.id }}</p>
            {% endif %}
            <div class="row">
                <h1 class="col-lg-12 text-center text-uppercase my-3">{{ article.titre }}</h1>
            </div>

            
        <div class="row">
            <img src="{{ asset('img/') }}{{ article.featuredImage }}" alt="" class="card-img-top" alt="..."
            style="width:100; height:250px; object-fit:cover;">
        </div>

        <p class="font-weight-bold mt-2" style="font-size: 25px;">{{ article.accroche }}</p>
        <p class="text-justify" style="font-size: 16px;">{{ article.contenu }}</p>
</body>


</div>
{% endblock %}
